{{!-- this template will render a form with fields for a transaction, it will dress the form with data from the {{user}}
that is available. It will need client side javascript to event handle the form submit by taking the form field data,
validating it, then sending it in a PUT request to "/profile/balance". It will handle the usual responses, code 200 -
all good, 400 user did something bad, 500 server broke --}}

<header>
    <nav>
        <div class="nav-wrapper teal">
            <img src="/images/seethroughbudgie.png" alt="Outline of budgie with 100 dollar note superimposed over it"
                style="width:60px;height:60px;">
            <a href="#" class="brand-logo center">UPDATE BALANCE</a>
        </div>
    </nav>
</header>
<main class="valign-wrapper">
    <div class="container">
        <div class="col-md-6">
            <form class="form update-balance-form center-align">
                <div class="form-group">
                    <label for="current-balance">CURRENT BALANCE</label>
                    <input class="form-input center-align" type="text" id="current-balance" value="{{user.balance}}"/>
                </div>
                <button id="update-btn" type="submit" class="btn">Update</button>
                <button id="cancel-btn" type="cancel" class="btn" onclick="window.history.back()"
                    class="btn btn-light mx-3 w-50">Cancel</button>
            </form>
        </div>
    </div>

  <script src="/js/update-post.js"></script>
</main>

{{!-- <main class="container">
    <section class="card login-card row g-3 align-items-center m-4">
        <div class="card-body d-flex flex-column p-4 w-75">
            <div>
                <h1 class="card-title text-center">Update Balance</h1>
                <form class="update-balance-form form">
            </div>
            <div class="form-group p-4">
                <label for="current-balance" class="form-font m-2">Current Balance</label>
                <input type="text" class="form-control" id="current-balance" placeholder="Decimal Value"
                    value="{{user.balance}}">
            </div>
            <div class="login-buttons form-group p-4 m-4 d-flex justify-content-evenly">
                <button id="update-btn" type="submit" class="btn btn-light mx-3 w-50">Update</button>
                <button id="cancel-btn" type="cancel" onclick="window.history.back()"
                    class="btn btn-light mx-3 w-50">Cancel</button>
            </div>
        </div>
        </form>
    </section>
</main> --}}

<script src="/js/update-balance.js"></script>